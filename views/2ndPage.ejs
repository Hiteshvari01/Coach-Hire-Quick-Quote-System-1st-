
<!DOCTYPE html> 
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>2nd Page</title>
  
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Lato:wght@700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
  <!-- Font Awesome & Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  
  
  <style>
    .btn-custom.active {
       background: linear-gradient(180deg, #2379F5 0%, #0A60DC 100%) !important;
      color: white !important;
    }

    .btn-custom.inactive{
      background-color: #F0F2F4 !important;
      color: #242628 !important;
    }
  </style>
</head>
<body class="bg-gray-100 font-[Inter]">

  <div class="w-full min-h-screen bg-[#003a7a] relative overflow-hidden pb-12">
    <!-- Background Image -->
    <img src="\assets\bg-bus.png" alt="Road Background" class="w-full h-auto block" />

    <!-- Gradient Section -->
    <div class="w-full bg-[#216fdb] px-4 py-10 -mt-4">
      <div class="max-w-[1200px] mx-auto flex flex-col md:flex-row md:items-start md:justify-center md:gap-12 lg:gap-16">

        <!-- Left Side -->
        <div class="flex flex-col items-center text-center max-w-[500px] w-full mt-12 md:mt-0">
          <img src="/assets/image 1.png" alt="Coach Hire Company" class="max-w-[180px] mb-8 md:mb-10 mx-auto" />
          <div class="text-white">
            <h1 class="font-[Lato] text-3xl md:text-4xl lg:text-5xl font-bold leading-tight mb-4 md:mb-6">
              Coach Hire Company<br />West Cork
            </h1>
            <p class="text-[#c0d8fa] text-xs sm:text-sm md:text-base lg:text-lg leading-tight sm:leading-relaxed md:leading-relaxed max-w-[450px] mx-auto md:mx-0">
             Based in Skibbereen, Dave Long Coach Travel is West Cork’s leading Coach & Mini Bus hire company. We pride ourselves on offering a wide range of coach and mini bus sizes to suit any group size or budget.
         </p>

          </div>
        </div>

        <!-- Right Side -->
         <div class="bg-white shadow-lg rounded-3xl w-full max-w-[480px] -mt-16 relative z-10">
          <h2 class="font-[Lato] font-bold text-center text-xl md:text-2xl lg:text-3xl px-6 py-8">
            Need A Quick Price Quote for Bus Hire in West Cork?
          </h2>

          <!-- Step Indicator -->
          <div class="flex items-center justify-center flex-wrap gap-2 mb-6">
            <div class="w-[22px] h-[22px] text-xs flex items-center justify-center rounded-full bg-blue-600 text-white">1</div>
            <div class="w-[50px] border-t border-gray-300"></div>
            <div class="w-[22px] h-[22px] text-xs flex items-center justify-center rounded-full bg-blue-600 text-white">2</div>
            <div class="w-[50px] border-t border-gray-300"></div>
            <div class="w-[22px] h-[22px] text-xs flex items-center justify-center rounded-full border border-gray-300 text-gray-500">3</div>
            <div class="w-[50px] border-t border-gray-300"></div>
            <div class="w-[22px] h-[22px] text-xs flex items-center justify-center rounded-full border border-gray-300 text-gray-500">4</div>
          </div>

          <form class="needs-validation" novalidate  id="stopsForm" method="POST" action="/save-stops">
            <input type="hidden" id="extraStopChoice" name="extraStopChoice" value="yes" />
               <input type="hidden" name="tripType" value="<%= tripType %>">

             <input type="hidden" name="tripId" value="<%= tripId %>">
             <div class="w-100" style="max-width: 500px;">
              <div class="mb-3 p-3" style="font-size: 17px; font-family: Inter, sans-serif; font-weight: 600; color: #242628;">
                Do you plan to make extra stops along the way?
              </div>

              <div class=" btn-group flex flex-wrap flex-col md:flex-row justify-center gap-2 mb-[10px]">
               <div id="btnYes" class="btn-custom btn-custom-yes flex items-center justify-center gap-2 w-full md:w-[42%] min-w-[14rem] px-3 py-2 text-[18px] font-medium font-sans rounded-sm cursor-pointer select-none min-w-[120px]" role="button" tabindex="0">
               <i class="fa-solid fa-check"></i> Yes
                </div>
              <div id="btnNo" class="btn-custom btn-custom-no flex items-center justify-center gap-2 w-full md:w-[42%] min-w-[14rem] px-3 py-2 text-[18px] font-medium font-sans rounded-sm cursor-pointer select-none min-w-[120px]" role="button" tabindex="0">
            <i class="fa-solid fa-xmark"></i> No
          </div>
          </div>

                       
              <div class="form-subsection p-3" style="display: none;">
                <p class="fw-semibold mb-1">Extra stops on the way there</p>
                <small class="text-muted d-block mb-3">
                  Provide addresses for each stop you wish to have, and the duration in minutes.
                </small>

               
      <!-- Extra Stops Container -->
<div id="extraStopContainer" class="space-y-4">

  <div class=" mb-2 flex items-start gap-2 stop-row w-full flex-nowrap">
    <!-- Stop Location -->
    <div class="flex-1 min-w-0">
      <label class="block text-sm font-medium mb-1">Stop Location</label>
      <div class="relative w-full">
        <input
          type="text"
          name="location[]"
          placeholder="Start typing an address or Eircode..."
          class=" form-control w-full rounded-lg border border-gray-300 py-2.5 px-2 text-xs pr-8 focus:outline-none focus:ring-2 focus:ring-blue-400 bg-gradient-to-b from-gray-100 to-white placeholder-transparent md:placeholder:text-xs"
          required
        />
        <i class="bi bi-geo-alt-fill absolute right-2 top-1/2 -translate-y-1/2 text-blue-600 cursor-pointer text-xs"></i>
      </div>
    </div>

    <!-- Duration + Remove Button in same column -->
    <div class="flex flex-col w-28 flex-shrink-0">
      <label class="block text-sm font-medium mb-1">Duration (min)</label>
      <div class="flex items-center gap-1">
        <div class="relative flex-1">
          <input
            type="text"
            name="duration[]"
            placeholder="Min"
            class=" form-control w-full rounded-lg border border-gray-300 py-2.5 px-2 pr-8 text-xs focus:outline-none focus:ring-2 focus:ring-blue-400 placeholder-transparent md:placeholder:text-xs bg-gradient-to-b from-gray-100 to-white"
            oninput="this.value = this.value.replace(/[^0-9]/g,'')"
            required
          />
          <i class="bi bi-clock-fill absolute right-2 top-1/2 -translate-y-1/2 text-blue-600 text-xs"></i>
        </div>

        <!-- Remove button outside input but in same column -->
        <button type="button" class="w-5 h-5 flex-shrink-0 flex items-center justify-center bg-gray-300 rounded-full hover:bg-red-500 hover:text-white remove-stop-btn text-xs">
          <i class="fa fa-times"></i>
        </button>
      </div>
    </div>
  </div>

</div>




                <!-- Add Stop Button -->
                <div id="addStopbtnWrapper">
                  <div class="add-stop-btn mb-3 text-[#0a67e6] font-[530] text-[16px] cursor-pointer" id="addStopBtn" style="cursor:pointer;">+ Add stop</div>
                </div>
           <% if (typeof tripType !== 'undefined' && tripType === 'return'){ %>
                <!-- Same stops checkbox -->
                <div class="form-check mb-3">
                  <input class="form-check-input" type="checkbox" id="sameStops" />
                  <label class="form-check-label fw-semibold" for="sameStops">
                    Same stops on the way home
                  </label>
                </div>

                <!-- Return stops -->
                <div id="returnStopSection">
                  <div class="fw-semibold mb-2">Extra stops on the return trip</div>
                  <div id="returnStopContainer">
                    <!-- Return stops inserted here dynamically -->
                    <div class="row g-2 mb-1 stop-row align-items-end">
                      <input type="hidden" name="stopType[]" value="return" />
                       <div class=" mb-2 flex items-start gap-2 stop-row w-full flex-nowrap">
    <!-- Stop Location -->
    <div class="flex-1 min-w-0">
      <label class="block text-sm font-medium mb-1">Stop Location</label>
      <div class="relative w-full">
        <input
          type="text"
          name="location[]"
          placeholder="Start typing an address or Eircode..."
          class=" form-control w-full rounded-lg border border-gray-300 py-2.5 px-2 text-xs pr-8 focus:outline-none focus:ring-2 focus:ring-blue-400 bg-gradient-to-b from-gray-100 to-white placeholder-transparent md:placeholder:text-xs"
          required
        />
        <i class="bi bi-geo-alt-fill absolute right-2 top-1/2 -translate-y-1/2 text-blue-600 cursor-pointer text-xs"></i>
      </div>
    </div>

    <!-- Duration + Remove Button in same column -->
    <div class="flex flex-col w-28 flex-shrink-0">
      <label class="block text-sm font-medium mb-1">Duration (min)</label>
      <div class="flex items-center gap-1">
        <div class="relative flex-1">
          <input
            type="text"
            name="duration[]"
            placeholder="Min"
            class=" form-control w-full rounded-lg border border-gray-300 py-2.5 px-2 pr-8 text-xs focus:outline-none focus:ring-2 focus:ring-blue-400 placeholder-transparent md:placeholder:text-xs bg-gradient-to-b from-gray-100 to-white"
            oninput="this.value = this.value.replace(/[^0-9]/g,'')"
            required
          />
          <i class="bi bi-clock-fill absolute right-2 top-1/2 -translate-y-1/2 text-blue-600 text-xs"></i>
        </div>

        <!-- Remove button outside input but in same column -->
        <button type="button" class="w-5 h-5 flex-shrink-0 flex items-center justify-center bg-gray-300 rounded-full hover:bg-red-500 hover:text-white remove-stop-btn text-xs">
          <i class="fa fa-times"></i>
        </button>
      </div>
    </div>
  </div>



                    </div>
                  </div>
                  <div id="returnAddStopBtn" class="add-stop-btn mb-3 text-[#0a67e6] font-[530] text-[16px] cursor-pointer" style="cursor:pointer;">+ Add stop</div>
                </div>
                 <% } %>
              </div>
             
              <hr class="my-6" />
             <button type="submit" class="bg-green-600 hover:bg-green-700 text-white rounded-full shadow-lg py-3 w-full max-w-[260px] mx-auto block text-base font-medium mb-2">
              Continue
            </button>
            </div>
            
          </form>
        </div>
      </div>
    </div>

    <!-- Section Title -->
    <h1 class="text-white text-center font-black py-16 text-2xl md:text-4xl lg:text-6xl">What We Do</h1>
  </div>

<script src="/js/2index.js"></script>
</body>
</html>


